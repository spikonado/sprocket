/** One Convex listPage / thread-cache page. Matches the local snapshot page size. */
export const HOSTED_THREAD_PAGE_SIZE = 40;
/** Caps client snapshot walks and listPage watch ranges so a bad cursor cannot loop forever. */
export const HOSTED_THREAD_LIST_MAX_PAGES = 250;
