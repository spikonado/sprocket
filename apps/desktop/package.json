{
	"name": "@sprocket/desktop",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"main": "./main.mjs",
	"desktopName": "com.spikonado.sprocket.desktop",
	"scripts": {
		"dev": "cargo build -q -p sprocket-cli && node ./launch.cjs",
		"build": "cargo build --release -p sprocket-cli && bun run --cwd ../web build && electron-builder"
	},
	"build": {
		"appId": "com.spikonado.sprocket",
		"productName": "Sprocket",
		"executableName": "sprocket",
		"icon": "../web/static/favicon.png",
		"directories": {
			"output": "dist"
		},
		"files": [
			"main.mjs",
			"preload.cjs",
			"package.json",
			{
				"from": "../web/dist/",
				"to": "web/dist/",
				"filter": [
					"**/*"
				]
			},
			{
				"from": "../../target/release/sprocket",
				"to": "server/sprocket"
			}
		],
		"linux": {
			"target": [
				"AppImage"
			],
			"category": "Development",
			"syncDesktopName": true
		}
	},
	"devDependencies": {
		"electron": "^41.5.0",
		"electron-builder": "^26.15.3"
	}
}
