# To use:
#
#     prek run -a
#
# Or:
#
#     prek install --prepare-hooks -f  # (runs every time you commit in git)
#
# To update this file:
#
#     prek autoupdate
#
# See https://prek.j178.dev/

repos:
  - repo: builtin
    hooks:
      - id: check-added-large-files
      - id: check-case-conflict
      - id: check-json
      - id: check-merge-conflict
      - id: check-symlinks
      - id: check-yaml
      - id: check-xml
      # Vendored skills stay byte-identical to upstream; never auto-fix them.
      - id: end-of-file-fixer
        exclude: ^crates/sprocket-workspace/(\.agents/|skills-lock\.json$)
      - id: fix-byte-order-marker
        exclude: ^crates/sprocket-workspace/(\.agents/|skills-lock\.json$)
      - id: mixed-line-ending
        exclude: ^crates/sprocket-workspace/(\.agents/|skills-lock\.json$)
      - id: trailing-whitespace
        exclude: ^crates/sprocket-workspace/(\.agents/|skills-lock\.json$)

  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.3
    hooks:
      - id: codespell
        exclude: ^bun\.lock$
        exclude_types: [svg]

  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.38.0
    hooks:
      - id: check-github-actions
        args: ['--verbose']
      - id: check-github-workflows
        args: ['--verbose']

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo-fmt
        language: system
        entry: cargo fmt
        pass_filenames: false

      - id: cargo-check
        name: cargo-check
        language: system
        entry: cargo check
        pass_filenames: false

      - id: commitlint
        name: commitlint
        language: system
        entry: commitlint --edit
        stages: [commit-msg]

      - id: eslint
        name: eslint
        language: system
        entry: bun turbo run lint
        pass_filenames: false

      - id: svelte-check
        name: svelte-check
        language: system
        entry: bun turbo run check
        pass_filenames: false

      - id: prettier
        name: prettier
        language: system
        entry: bun prettier --write .
        pass_filenames: false
